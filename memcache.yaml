interface: memcache
description: |
  memcache service
providing:
- 
  field: hostname
  description: the hostname for the memcache server
  type: string
  default: memcache service hostname
- 
  field: port
  description: the port for the memcache server
  type: string
  default: 11211
requiring:
- 
  field: hostname
  description: client hostname is added to memcached_ips
  type: string
  default: 
  required: true

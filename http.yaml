interface: website
description: |
  website interface
providing:
- 
  field: hostname
  description: the server hostname
  type: string
  default: exporting service hostname
- 
  field: port
  description: the server port
  type: string
  default: 80
requiring:

interface: rabbitmq-node
summary: |
  rabbitmq-node peer interface
description: |
  RabbitMQ is an implementation of AMQP, the emerging standard for high
  performance enterprise messaging. The RabbitMQ server is a robust and
  scalable implementation of an AMQP broker.
providing:
requiring:
- 
  field: host
  summary: the host to use for rabbitmq access
  type: string
  default: peer service hostname
  required: true
- 
  field: cookie
  summary: 
  type: string
  default: generated
  required: true

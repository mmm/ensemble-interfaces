interface: rabbitmq-node
summary: |
  rabbitmq-node
providing:
requiring:
- 
  field: host
  summary: the password to use for database access
  type: string
  default: generated
  required: true
- 
  field: cookie
  summary: the hostname for the mysql server
  type: string
  default: mysql service hostname
  required: true

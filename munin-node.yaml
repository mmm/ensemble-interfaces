interface: munin
summary: |
  munin interface
providing:
- 
  field: ip
  summary: the hostname for the munin server
  type: string
  default: exporting service hostname
requiring:
- 
  field: ip
  summary: client ip address is needed for configuration and access control
  type: string
  default: 
  required: true
